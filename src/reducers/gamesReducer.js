const initialState = {
    games: [],
    loading: true
}

const gamesReducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default gamesReducer;
